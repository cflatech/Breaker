{
  "scripts": {
    "build": "tsc && esbuild src/* --bundle --sourcemap --outdir=scripts/",
    "watch": "esbuild src/* --bundle --sourcemap --outdir=scripts/ --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.198",
    "@typescript-eslint/eslint-plugin": "^5.40.0",
    "@typescript-eslint/parser": "^5.40.0",
    "esbuild": "^0.15.11",
    "eslint": "^8.25.0",
    "eslint-config-prettier": "^8.5.0",
    "prettier": "^2.7.1",
    "typescript": "^4.8.4"
  }
}
